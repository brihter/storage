# Examples

## Prerequisites

```
npm i @brighter/storage
```

## Example

```js
const { Storage } = require('@brighter/storage')

const storage = Storage({
  type: 'local',
  path: '/tmp/storage'
})
```
